<input  type="text" id="{{ label_for }}"
        data-custom="{{ field_custom_data }}"
        name="{{ field_name }}"
        value="{{ field_value }}"
        style="width: 22rem;">
<p>Note: Can be ANYTHING, please use descriptive format: PROJECT-ENVIRONMENT</p>
<p>E.g: <strong>BRK-STAGING</strong> for BuyRentKenya's staging blog</p>
